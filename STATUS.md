# 현재 상태 — 무엇이 되고, 무엇이 안 됐나

마지막 갱신: 2026-09-05 (개선 루프 종료 시점)
진행: **14개 항목 중 9개 완료** (A1~A6 접근성, B1 브로커, B2 에디터+diff, C1 API 키 등록).
남은 것: B3~B6, C2, 그리고 실제 실행 1회.

---

## 가장 먼저 알아야 할 것

**이 프로그램은 아직 실제 모델로 한 번도 돌아본 적이 없다.**
지금까지의 검증은 전부 `--mock`(대본 응답)이다. 즉 다음이 **미검증**이다:

- Anthropic `messages.parse()` 구조화 출력이 실제 스키마로 통과하는지
- Tool Runner가 실제로 `src/`에만 쓰는지 (거부 메시지를 모델이 이해하고 교정하는지)
- Gemini가 `response_schema`로 유효한 `TestSuite` / `QAVerdict`를 반환하는지
- QA가 쓴 테스트를 DEV가 실제로 통과시킬 수 있는지 (제일 불확실한 부분)
- 프롬프트 캐싱이 실제로 적중하는지

**C1(API 키 등록)을 끝내고 실제로 한 번 돌려보기 전까지는 "동작한다"고 말할 수 없다.**
자체 테스트 67개가 통과하는 것은 신뢰 경계와 격리가 지켜진다는 뜻이지,
에이전트 협업이 실제로 성공한다는 뜻이 아니다.

---

## 완료된 것

### 핵심 구조
- 파이썬 상태머신 오케스트레이션 (LLM이 라우팅하지 않음)
- PM(계획) / DEV(구현) / QA(검증) 3역할, 프로젝트 폴더로만 소통
- FastAPI + SSE로 대화를 실시간 스트리밍, 단일 HTML 홀로그래픽 UI

### 신뢰 경계 — 구현자는 검증기를 고칠 수 없다
- `src/`는 DEV만 쓰기, `tests/`는 QA만 쓰기 (DEV는 읽지도 못함)
- 테스트가 구현보다 **먼저** 작성됨 (`WRITE_TESTS` 단계)
- `conftest.py` / `pytest.ini` / `sitecustomize.py` / `usercustomize.py` / `*.pth` 전면 금지
- 경로 탈출 차단 (`Path.resolve()` 후 루트 비교 — 심링크까지 해석)

### 생성 코드 실행 격리 (`runner.py`)
- 환경변수 세탁 — API 키가 자식 프로세스에 안 넘어감 (실제 카나리 테스트로 확인)
- `PYTHONNOUSERSITE` + `-I` + 고정 `pytest.ini`(`-c`)로 자동 로딩 경로 차단
- 타임아웃 시 프로세스 트리 전체 종료 (`taskkill /T` / `killpg`)

### 측정
- 완성도에서 QA 통과율 제거 (순환논리 차단), 최종은 인수기준 충족률
- `기준 커버` — 테스트가 붙은 인수기준 비율
- 에이전트별 토큰·비용 계기판, 캐시 적중 여부 표시
- 호출 **전** 예산 검사, `MAX_REPLANS`, 태스크 지문으로 `done` 관리

### 접근성 (A1~A6)
랜드마크·스킵 링크·라이브 리전·모션 축소·대비 상향·포커스 트랩·대체 텍스트.
회귀 테스트 17개로 고정.

### 저장소
프로젝트별 폴더 + `.meta.json`, REST API.
파일 편집기(사람이 직접 수정) + 버전 이력 + diff — 에이전트 실행 중에는 편집 차단.

---

## 안 된 것 — 남은 작업

우선순위 순. 자세한 설계 메모는 [ROADMAP.md](ROADMAP.md)에 있다.

### 1순위 — 이걸 해야 실제로 쓸 수 있다

**~~C1. API 키 등록 화면~~ — 완료**
설정 화면(헤더의 "설정" 버튼)에서 키를 넣고 "연결 확인"을 누르면
Gemini 모델 목록을 실제로 조회해 드롭다운에 채운다.
`anthropic` / `google-genai` 패키지도 설치돼 있다.

**실제 실행 1회 + 실패 수습 — 맨 마지막으로 미룸**
사용자 지시: "API는 제일 나중에". 키 입력은 사용자가 직접 해야 한다(대신 넣어줄 수 없다).
B 항목을 모두 끝낸 뒤에 한다.
C1 직후 반드시 진짜로 한 번 돌려야 한다. 위 "미검증" 목록이 여기서 드러난다.
특히 QA가 쓴 테스트가 너무 엄격해 DEV가 계속 반려당하는 **데드락**이 가장 유력한 실패다.
`MAX_REPLANS`(3)에 걸려 중단되면 `tests/`를 직접 열어보고 QA 프롬프트를 조정할 것.

### 2순위 — Orca에서 가져오기로 한 것

**B3. 산출물 미리보기 패널**
`src/`에 HTML이 있으면 sandbox iframe으로 렌더. 아니면 진입점 stdout.

**B4. 병렬 작업공간**
현재 **한 번에 한 프로젝트만** 돈다. `bus`가 단일 전역이라 프로젝트별로 쪼개야 한다.
범위가 크니 착수 전에 항목을 나눌 것.

**B5. 협업 타임라인** — `logs/trace.jsonl`을 핸드오프 타임라인으로 시각화.

**B6. 예약 실행** — 정해진 시각에 저장된 요구사항 자동 실행.

### 3순위
**C2. 최종 점검** — README·BUILD_PLAN을 최종 상태와 맞추고 "막지 못하는 위험" 갱신.

---

## 고치지 못한 위험 — 정직하게

### 네트워크 송신을 막지 못한다 (가장 큰 것)
생성된 코드가 홈 디렉터리 파일을 읽어 외부로 보내는 것을 `subprocess`로는 차단할 수 없다.
API 키를 지워도 이 경로는 남는다. OS 수준 격리(컨테이너, 방화벽 규칙)가 필요하다.
**신뢰할 수 없는 요구사항을 돌릴 거라면 이 앱 자체를 컨테이너 안에서 실행할 것.**

### CPU/RAM 제한이 없다
타임아웃만 있다. 메모리를 폭식하는 코드는 타임아웃 전에 시스템을 먹는다.

### 프롬프트 캐싱이 실제로 걸리는지 모른다
시스템 프롬프트에 `cache_control`을 붙였지만, 프리픽스가 모델의 최소 캐시 길이
(512~4096 토큰) 미만이면 **조용히 무시된다**. 한국어 프롬프트가 그 경계선에 있다.
계기판의 `캐시 읽기`가 계속 0이면 캐싱은 안 걸리는 것이고,
그 상태에서 "캐싱으로 비용을 아꼈다"고 말하면 근거 없는 주장이다.

### Gemini 단가가 근사치
`config.PRICES`의 `gemini-2.5-pro: (1.25, 10.00)`은 참고값이다. 비용 표시가 부정확하다.

### QA가 쓴 테스트가 틀릴 수 있다
너무 엄격하면 정상 구현이 영원히 반려되고(데드락), 너무 느슨하면 틀린 구현이 통과한다.
지금은 `MAX_REPLANS`가 유일한 탈출구다. 테스트-인수기준 매핑(`covers`)은 넣었지만
"AC는 있는데 검증하는 테스트가 없음"을 자동으로 막지는 않는다 — `기준 커버` 숫자로 보여줄 뿐.

---

## 프로세스에서 배운 것

**ChatGPT 교차검토가 2회 연속 실패했다.**
1회차·2회차 모두 새로 던진 질문이 아니라 **직전 라운드 질문에 답했고**,
근거 없는 동의만 돌아왔다. 대화가 길어지며 컨텍스트가 밀린 것으로 보인다.
다시 할 거라면 **항목마다 새 대화를 열어 짧게 묻는 방식**으로 바꿀 것.

초반 2라운드는 실제로 유용했다 — DEV가 테스트를 조작할 수 있다는 점,
API 키가 생성 코드에 노출된다는 점, `sitecustomize.py` 우회 경로는
그쪽 지적으로 잡았다. 반대로 심링크·타임아웃 지적은 이미 처리돼 있어 반박했고 철회받았다.

**20분 cron 루프는 거의 발화하지 않았다.**
세션이 유휴일 때만 도는데, 대화를 주고받는 동안에는 기회가 없었다.
자동 루프보다 **연속으로 직접 처리하는 편이 실제로 더 진도가 나갔다.**

---

## 다시 시작할 때

```bash
pip install -r requirements.txt
python -m pytest tests_selftest.py -q     # 67개 통과해야 정상
python server.py --mock                    # http://127.0.0.1:8000
```

C1이 끝났으니 **다음은 실제 실행 1회**다. 서버를 키 없이 띄운 뒤(`python server.py`,
--mock 없이) 헤더의 "설정"에서 키를 등록하고 "연결 확인"까지 누른 다음 요구사항을 넣는다.
B2~B6은 그 실행이 통과한 뒤에 하는 게 맞다. 검증되지 않은 토대 위에 기능을 얹지 말 것.
