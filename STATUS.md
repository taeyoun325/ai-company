# 현재 상태 — 무엇이 되고, 무엇이 안 됐나

마지막 갱신: 2026-09-21 · **DAY 19**

---

## 가장 먼저 알아야 할 것

**아직 실제 모델로 한 번도 완주하지 않았다.** 키가 없기 때문이다.

DAY 14 에 유효하지 않은 키로 실제 SDK 를 태워봤고, 거기서 진짜 버그가
나왔다 — 현재 Claude 모델은 `temperature` 를 받지 않는다(`output_config.effort`
가 그 자리다). 키를 꽂는 날 첫 호출에서 터졌을 버그이고, 그때까지
자체 테스트 392개가 전부 통과하고 있었다. **Mock 은 SDK 시그니처를 모른다.**

401 까지는 확인했다. 200 은 못 봤다 — 응답 파싱·사용량 해석·스트리밍은
실물로 검증되지 않았다.

### 키를 받으면 (순서대로)

```bash
curl localhost:8000/api/preflight        # 1. 점검 (호출 없음, 무료)
python scripts/first_real_run.py         # 2. 제공자별 최소 호출
pytest backend/tests/test_providers.py   # 3. 보류 15개가 자동으로 걸린다
python scripts/first_real_run.py --company   # 4. 실제 직원으로 프로젝트 하나
```

---

## 되는 것

| | 상태 |
|---|---|
| AUTO 전 구간 (§9 §10) | ✅ 기획→테스트 선작성→구현→pytest→교차검증→재작업→검수 |
| MANUAL (§11) | ✅ 직원 지목·직접 지시·요청 시 검증 |
| AI 직원 5명 (§8) | ✅ 권한 경계가 코드로 강제됨 |
| 제공자 3사 (§7) | ✅ Claude · Gemini · OpenAI + Mock |
| 실시간 로그 (§13) | ✅ SSE + 재연결 이어받기 + 폴링 폴백 |
| 화면 (§4 §12 §16) | ✅ 랜딩 · 사무실 · 프로젝트 · 산출물/diff · MANUAL · 요금제 · 설정 |
| 크레딧·요금제 (§15 §16) | ✅ 잔액이 실제로 줄고 실제로 막는다 · AUTO 와 MANUAL 둘 다 |
| 단가 (§14) · 원가 (§17) | ✅ 공식 문서 대조 · 원가 비율 35% 이하로 재조정 |
| **요금제 5종 · BYOK** | ✅ 요금제가 **누구의 키로 부를지**를 정한다 |
| 프로젝트 저장 (§12) | ✅ 파일이 진실, SQLite 색인은 사본 |
| 안전장치 (§18) | ✅ 라운드·재작업·재기획·비용·동시실행·크레딧 |
| 배포 자세 | ✅ `DEPLOY_MODE=saas` 에서 로컬 접근 전면 차단 |
| **인증 · 테넌트 분리** | ✅ 이메일+비밀번호. `owner` 는 세션에서만 나온다 |
| **컨테이너 배포** | ⚠️ 파일은 썼지만 **docker 가 없어 실행 검증 못 함** |
| **실제 모델 완주** | ❌ **키 없음** |

자체 테스트 **488 통과 / 15 보류**. 보류 15개는 실제 제공자 계약
테스트다 — 키가 들어오는 순간 자동으로 걸린다.

---

## 날짜별로 한 일 · 그날 잡은 것

| | 한 일 | 잡은 것 |
|---|---|---|
| 1~2 | 구조 전환, Provider 추상화 | legacy 가 import 조차 안 되는 상태 |
| 3 | Gemini · OpenAI, 모델 카탈로그 | 카탈로그 모델 3개의 단가 누락 |
| 4 | 직원 5명, 권한, JSON 파서 | 파서 결함 2개 (스키마 이름, 중첩 코드펜스) |
| 5 | Orchestrator, AUTO | — |
| 6 | MANUAL | 지시마다 사용량이 0으로 돌아감 |
| 7 | SSE 재정비 | approvals 의 `id` 가 이벤트 번호를 덮어씀 |
| 8~10 | 프론트엔드 | SSE 프록시 버퍼링 · slug 이중 인코딩 · 사용량 0 · 하이드레이션 |
| 11 | 단가·크레딧·요금제 | 캐시 단가 오류 · **무료 요금제가 성립 안 함** |
| 12 | 프로젝트 저장·색인 | — |
| 13 | 보안 — SaaS 전제 | 승인 게이트가 SaaS 에서 무의미 |
| 14 | 프리플라이트·실물 준비 | **temperature 가 SDK 에 없다** |
| 15 | 인증·테넌트 분리 | Secure 쿠키가 http 에서 안 감 · 폼 오류가 연결 오류로 표시 |
| 16 | 배포 준비 | — (네트워크 격리를 **새로 막았다**) |
| 17 | 이전 제품 잔재 정리 | — (main.py 950→756줄) |
| 18 | 공개 랜딩 | — |
| 19 | 요금제 재설계 · BYOK | **MANUAL 이 크레딧을 한 개도 안 깎았다** · 운영자 키 설정이 SaaS 에서 무방비 |

---

## 아직 안 한 것 · 남은 위험

### 실제 모델 완주 0회
최대 미검증 구간. 위 참조.

### 컨테이너를 띄워본 적이 없다
이 머신에 docker 가 없다(DAY 1·16 확인). `Dockerfile` · `docker-compose.yml`
은 의도를 맞춰 썼지만 **한 번도 뜬 적이 없다.** 처음 올릴 때는 실패를
예상하고 `docker compose logs -f` 를 볼 것. → [docs/deploy.md](docs/deploy.md)

### 비밀번호 재설정 · 이메일 인증이 없다
발송 경로(SMTP·SES 등)를 붙이지 않았다. 지금은 비밀번호를 잊으면 복구할
방법이 없고, 아무 이메일 주소로나 가입할 수 있다.

### 결제가 없다
크레딧은 실제로 줄고 실제로 막지만 충전은 데모 버튼이다. 화면에도 그렇게
적혀 있다.

### 가격은 실측 위에 있지 않다
요금제는 DAY 19 에 이렇게 정했다:

| | 월 | 크레딧 | 누구의 키 | 최악 원가 비율 |
|---|---|---|---|---|
| 무료 | $0 | — | **Mock** (실제 호출 없음) | 0 |
| 스타터 | $9 | 250 | 운영자 | 27.8% |
| 프로 | $29 | 900 | 운영자 | 31.0% |
| 비즈니스 | $99 | 3,200 | 운영자 | 32.3% |
| 자체 키 | $19 | — | **고객** (직접 결제) | 0 (모델 한정) |

이 표의 **분모를 아직 모른다.** 프로젝트 1건의 실제 원가를 한 번도 재본
적이 없다(실제 모델 완주 0회). 화면의 "월 N건"은 호출 20~40회 × $0.03~0.15
라는 계산에서 나온 **추정**이고, 화면에도 추정이라고 적혀 있다. 키가
들어오면 10건을 돌려 중앙값·p90 을 뽑고 크레딧 수를 확정해야 한다.

원가 비율 상한은 0.50 → **0.35** 로 조였다. 0.50 을 지키던 이전 숫자
(프로 41.4% · 비즈니스 45.5%)에는 API 원가만 있었고, 결제 수수료
(2.9%+$0.30) · 서버 · 환불 · 지원 · 모델 단가 인상 여지를 더하면 49%
근처로 벽에 붙었다. 벽에 붙은 값은 여유가 아니라 우연이다.

### 고객 키 암호화 키가 같은 서버에 있을 수 있다
`BYOK_SECRET` 을 환경변수로 넣지 않으면 서버가 파일에 KEK 를 만들어 쓴다.
서버 디스크를 가져간 사람은 고객 키도 가져간다. 설정 화면이 고객에게 그
사실을 그대로 보여준다. → [docs/security.md](docs/security.md)

### 인스턴스를 하나만 띄워야 한다
세션 일부·실행 상태·MANUAL 대화 이력이 프로세스 메모리에 있다.
로드밸런서 뒤에 두 개를 띄우면 요청마다 다른 인스턴스로 가면서 어긋난다.

### 생성된 코드 격리는 리눅스에서만
DAY 16 에 `unshare --net` 으로 네트워크를 끊었지만 **리눅스 전용**이고,
커널이 비특권 네임스페이스를 막아두면 안 된다. 되는지 여부는 테스트
리포트의 `network_isolated` 로 나간다 — 조용히 꺼지지는 않는다.
디스크·메모리 제한은 여전히 컨테이너의 몫이다.

### 프롬프트 주입은 막지 못한다
줄였을 뿐이다. 진짜 방어는 구조 쪽에 있다 — 설득당한 직원도 권한 밖
파일은 못 쓴다. → [docs/security.md](docs/security.md) §3

### PostgreSQL 이전을 실제로 해보지 않았다
SQL 은 표준으로 썼고 고칠 곳은 `_connect()` 와 자리표시자 둘뿐이라고
적어뒀지만, 옮겨보지는 않았다.

### 구글 로그인은 나중
계정 구조(`identities` 테이블)는 테이블 변경 없이 끼울 수 있게 만들어뒀다.
