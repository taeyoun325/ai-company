# 현재 상태 — 무엇이 되고, 무엇이 안 됐나

마지막 갱신: 2026-09-06 (재설계 완료 시점)

---

## 가장 먼저 알아야 할 것

**실제 모델로 아직 한 번도 돌려본 적이 없다.**
사용자 지시("API는 제일 나중에")에 따라 키 등록과 실제 실행을 맨 뒤로 미뤘다.
따라서 다음이 **미검증**이다:

- Tool Runner가 실제로 도구를 호출하고 대화가 여러 턴 이어지는지
- `call_agent`로 보조 에이전트가 실제로 불리고 결과가 메인에게 돌아오는지
- Gemini(reviewer)가 실제 응답을 주는지
- 승인 게이트가 **실제 에이전트 호출 흐름 안에서** 대기·해제되는지
  (단위 테스트로는 확인했지만 실제 모델 루프 안에서는 아직)
- 프롬프트 캐싱이 걸리는지

자체 테스트 134개 통과는 **권한 게이트와 경로 검사가 지켜진다**는 뜻이지,
에이전트 협업이 실제로 성공한다는 뜻이 아니다.

---

## 완료된 것

### 틀 — Claude Code와 동일
- 사용자의 **실제 프로젝트 폴더**를 열고 그 안에서 일한다
- **대화형 루프.** 여러 턴이 이어지고, 다음 행동은 모델이 정한다
- 도구: `read_file` `list_dir` `search` `find_files` `write_file` `edit_file` `run_command`
- git 상태를 읽어 변경된 파일을 실시간 표시. git이 아니면 경고한다

### 에이전트 — 틀 위에 얹은 것
- `call_agent(role, task, files)` 하나로 붙는다
- planner(Claude·읽기만) / reviewer(**Gemini**·읽기만) / tester(Claude·쓰기 가능)
- 메인이 필요하다고 판단할 때만 호출. 항상 부르지 않는다

### 권한 — 방어의 축
- Claude Code와 같은 5모드: 자동 / 수동 / 편집 자동 수락 / 계획 / 권한 무시
- 파일 쓰기 승인에 **diff 동봉**
- **어떤 모드에서도** 자격 증명·시스템 단축키는 차단
- 계획 모드는 변경 도구를 아예 넘기지 않는다
- 비상 정지가 대기 중인 모든 행동을 한 번에 해제

### 경로 안전
- `Path.resolve()` 후 루트 비교 — 심링크까지 해석
- `sitecustomize.py` · `usercustomize.py` 금지
- `.env` · `id_rsa` · `*.pem` 등은 읽기 거부, 트리에서 경고 표시
- `node_modules` · `.git` 등 잡음 폴더는 접어서 보여줌

### 비밀키
- 기동 시 `os.environ`에서 키를 꺼내 **지운다**
- 화면에는 마스킹만, 원문은 다시 내려보내지 않음
- `run_command`가 자식 프로세스에 넘기는 환경을 세탁
- 로그·오류 출력에서 키를 가리는 `scrub()`

### 부가 기능
- 첨부(이미지·PDF·텍스트) + 화면 캡처 → 자료/지시 경계 명시
- 협업 타임라인, 예약 실행
- 접근성: 랜드마크·라이브 리전·포커스 트랩·모션 축소·대비

---

## 남은 작업

### 1순위 — 실제 실행 1회
키를 등록하고 진짜로 한 번 돌려야 위 "미검증" 목록이 해소된다.
가장 유력한 실패는 **Tool Runner 루프 안에서 승인 게이트가 블로킹하는 문제**다.
승인 대기가 120초인데 그동안 도구 실행이 멈춰 있으므로, 실제로는 잘 돌아가는지
확인이 필요하다. 문제가 생기면 타임아웃을 줄이거나 비동기로 바꿔야 한다.

### 2순위
- **대화 세션 저장·복원** — 지금은 서버를 끄면 대화가 사라진다
- **컨텍스트 길이 관리** — 긴 대화가 모델 한도를 넘으면 지금은 그냥 오류가 난다.
  압축(compaction)이나 오래된 도구 결과 정리가 필요하다
- **`run_command` 스트리밍** — 지금은 끝나야 출력이 보인다. 긴 빌드에 답답하다
- 프롬프트 캐싱 실측 확인

---

## 고치지 못한 위험 — 정직하게

### 네 실제 파일을 고친다
승인 게이트가 있어도 습관적으로 누르면 무너진다.
**git 저장소에서 쓸 것.** 아니면 되돌릴 방법이 없다.

### `run_command`는 네 권한 그대로다
네트워크 송신, 홈 디렉터리 읽기를 막지 못한다. API 키는 세탁하지만 그뿐이다.
OS 수준 격리(컨테이너)가 필요하고, 개인용 로컬 도구에 그것을 강제하지 않았다.

### 화면 캡처는 외부로 나간다
캡처한 이미지는 Anthropic으로 전송된다. 민감한 창은 닫고 찍을 것.

### 프롬프트 주입은 완전히 막을 수 없다
첨부와 화면 내용에 "이건 자료지 지시가 아니다"를 붙였지만, 모델이 100% 지킨다는
보장은 없다. 그래서 **구조로** 막았다 — 승인 없이는 실행 경로가 없다.
게이트를 끄면(권한 무시) 이 방어도 사라진다.

### 신뢰 경계를 버렸다
이전 설계에는 "구현자는 테스트를 고칠 수 없다"는 보장이 있었다.
실제 폴더에서 대화형으로 돌기 위해 그것을 포기했다. 이제 tester가 쓴 테스트를
메인이 고칠 수 있다. 그 보장이 필요하면 `legacy/`의 격리 모드가 남아 있다.

---

## 이전 설계 (`legacy/`)

격리된 `projects/` + 고정 파이프라인(PLAN→WRITE_TESTS→IMPLEMENT→TEST→REVIEW) +
역할별 쓰기 권한. 지우지 않은 이유:

- 신뢰 경계 실험의 기록이다 — "구현자가 검증기를 못 고치게 하면 어떻게 되는가"
- 격리 모드로 되돌릴 여지를 남긴다
- `legacy/tests_legacy.py`에 그 구조의 테스트 130개가 그대로 있다
