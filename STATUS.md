# 현재 상태 — 무엇이 되고, 무엇이 안 됐나

마지막 갱신: 2026-09-21 · **DAY 14 / 14**

---

## 가장 먼저 알아야 할 것

**아직 실제 모델로 한 번도 완주하지 않았다.**

키가 없기 때문이다. 방침("API 키는 제작을 마친 뒤 맨 마지막에")대로
DAY 13까지 전 구간을 Mock 으로 만들었고, 마지막 날인 오늘 키를 받지 못했다.
그래서 이 리스크는 **줄었지만 사라지지 않았다.**

무엇이 줄었나 — DAY 14 에 유효하지 않은 키로 실제 SDK 를 태워봤고,
거기서 진짜 버그가 하나 나왔다:

```
TypeError: Messages.create() got an unexpected keyword argument 'temperature'
```

현재 Claude 모델은 `temperature` 를 받지 않는다. 그 자리는
`output_config.effort` 다. **키를 꽂는 날 첫 호출에서 터졌을 버그**이고,
그 시점까지 자체 테스트 392개가 전부 통과하고 있었다. 이유는 단순하다 —
**Mock 은 SDK 시그니처를 모른다.**

그래서 `tests/test_sdk_contract.py` 를 추가했다. 각 어댑터가 만드는
페이로드의 키가 그 SDK 함수가 실제로 받는 인자 안에 있는지 검사한다.
키 없이 잡을 수 있는 마지막 구간이다.

**남아 있는 것**: 401 까지는 확인했지만 200 은 못 봤다. 응답 파싱·
사용량 해석·스트리밍은 실물로 검증되지 않았다.

### 키를 받으면 할 일 (순서대로)

```bash
# 1. 점검 — 실제 호출 없이 설정만 본다
curl localhost:8000/api/preflight

# 2. 최초 실물 호출 — 제공자별 1번씩. 싸다
python scripts/first_real_run.py

# 3. 계약 테스트 — 15개 skip 이 자동으로 풀린다
.venv/Scripts/python.exe -m pytest backend/tests/test_providers.py -q

# 4. 실제 직원들로 프로젝트 하나 (비쌈)
python scripts/first_real_run.py --company
```

---

## 되는 것

| | 상태 |
|---|---|
| AUTO 모드 전 구간 (§9 §10) | ✅ 기획→테스트 선작성→구현→pytest→교차검증→재작업→최종검수 |
| MANUAL 모드 (§11) | ✅ 직원 지목·직접 지시·요청 시 검증 |
| AI 직원 5명 (§8) | ✅ 권한 경계가 코드로 강제됨 |
| 제공자 3사 (§7) | ✅ Claude · Gemini · OpenAI + Mock. 교체는 설정 한 줄 |
| 실시간 로그 (§13) | ✅ SSE + 재연결 이어받기 + 폴링 폴백 |
| 화면 (§4 §12 §16) | ✅ 사무실 · 프로젝트 · 산출물/diff · MANUAL · 요금제 · 설정 |
| 크레딧·요금제 (§15 §16) | ✅ 잔액이 실제로 줄고 실제로 막는다 |
| 단가 (§14) | ✅ 공식 문서와 대조 완료 (2026-09-21) |
| 원가 비율 (§17) | ✅ 프로 41.4% · 비즈니스 45.5% (기준 50% 이하) |
| 프로젝트 저장 (§12) | ✅ 파일이 진실, SQLite 색인은 사본 |
| 안전장치 (§18) | ✅ 라운드·재작업·재기획·비용·동시실행·크레딧 |
| 배포 자세 | ✅ DEPLOY_MODE=saas 에서 로컬 접근 전면 차단 |
| **실제 모델 완주** | ❌ **키 없음** |

자체 테스트 **399 통과 / 15 보류**. 보류 15개는 실제 제공자 계약
테스트다 — 키가 들어오는 순간 자동으로 걸린다.

---

## 날짜별로 한 일

| | 한 일 | 그날 잡은 것 |
|---|---|---|
| 1 | 구조 전환, 환경 | legacy 가 import 조차 안 되는 상태 확인 |
| 2 | Provider 추상화, 재시도·백오프·대체 | Mock 은 대체 사슬에 넣지 않기로 |
| 3 | Gemini · OpenAI 어댑터, 모델 카탈로그 | 카탈로그에 넣은 모델 3개의 단가가 없었다 |
| 4 | 직원 5명, 권한, JSON 파서, Mock 대본 | 파서 결함 2개 (스키마 이름, 중첩 코드펜스) |
| 5 | Orchestrator 상태머신, AUTO | — |
| 6 | MANUAL 모드 | 지시마다 사용량이 0으로 돌아가던 것 |
| 7 | SSE 재정비 | approvals 의 `id` 가 이벤트 번호를 덮어씀 |
| 8~10 | 프론트엔드 전체 | SSE 프록시 버퍼링, slug 이중 인코딩, 사용량 0, 하이드레이션 |
| 11 | 단가 대조, 크레딧, 요금제, 원가 | 캐시 단가 오류 · **무료 요금제가 성립 안 함** |
| 12 | 프로젝트 저장·색인 | — |
| 13 | 보안 — SaaS 전제 반영 | 승인 게이트가 SaaS 에서 무의미하다는 것 |
| 14 | 프리플라이트, 실물 호출 준비 | **temperature 가 SDK 에 없다** |

---

## 아직 안 한 것 · 남은 위험

### 실제 모델 완주 0회
위에 적은 그대로. 여전히 이 프로젝트의 최대 미검증 구간이다.

### 다중 사용자 격리가 없다
소유자(`owner`)는 `"local"` 하나뿐이다. 인증·권한·테넌트 분리를 만들지
않았다. **지금 상태로 여러 사람에게 열면 서로의 프로젝트를 볼 수 있다.**
`docs/security.md` §3 에 적혀 있다.

### 결제가 없다
크레딧은 실제로 줄고 실제로 막지만, 충전은 데모용 버튼이다.
결제 연동은 범위 밖이라고 화면에도 적어뒀다.

### 생성된 코드 실행은 컨테이너가 필요하다
`SANDBOXED=1` 은 우리가 확인할 수 없는 **운영자의 선언**이다. 네트워크
송신 제한과 자원 제한을 컨테이너에 걸지 않고 켜는 것은 거짓 서명이다.

### docker 로 검증하지 못했다
이 개발 머신에 docker 가 없다(DAY 1 확인). `docker-compose.yml` 은
여전히 실행으로 검증되지 않았다. PostgreSQL 이전도 마찬가지다 —
SQL 은 표준으로 썼지만 실제로 옮겨보지는 않았다.

### 프롬프트 주입은 막지 못한다
줄였을 뿐이다. 진짜 방어는 구조 쪽에 걸어뒀다 — 설득당한 직원도
권한 밖 파일은 못 쓴다. `docs/security.md` §3.

### 이전 제품의 잔재가 남아 있다
`agents/core.py` · `agents/subagents.py` · `screen.py` · `scheduler.py` ·
`timeline.py` 는 이전 대화형 도구의 것이다. SaaS 모드에서는 막히지만
코드는 그대로 있다. 정리하면 읽기 쉬워지고, 안 하면 다음 사람이
"직원이 5명인가 8명인가"로 헷갈린다.
