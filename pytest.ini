[pytest]
pythonpath = backend
testpaths = backend/tests
# 테스트 클래스는 쓰지 않는다. 비워두지 않으면 스키마 이름(TestSuite)을
# 테스트 클래스로 오인해 수집 경고를 낸다 — 경고가 쌓이면 진짜 경고가 묻힌다.
python_classes =
markers =
    slow: 실제 서브프로세스를 띄우는 느린 테스트
