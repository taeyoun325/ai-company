# 개선 루프 작업 목록

20분 주기 루프가 이 파일을 위에서부터 하나씩 처리한다.
**한 회차 = 항목 하나.** 범위를 넘기지 않는다. 넘길 것 같으면 항목을 쪼개서 아래에 추가한다.

## 회차 절차 (매번 동일)

1. `상태: 대기` 인 첫 항목을 고른다.
2. 그 항목의 설계안을 ChatGPT에 보내 **반박을 받는다.** 동의만 받으면 실패로 친다.
   - 브라우저 세션이 끊겼으면 건너뛰고 혼자 판단한다. 로그에 그렇게 적는다.
3. 합의된 것만 구현한다. 반박이 틀렸으면 반박하고 근거를 기록한다.
4. **검증**: `python -m pytest tests_selftest.py -q` 와 mock 전체 파이프라인을 돌린다.
   깨졌으면 **되돌리고** 항목을 `상태: 실패`로 두고 이유를 적는다.
5. 이 파일의 항목 상태와 `## 기록`을 갱신한다.
6. **커밋하고 푸시한다.** `git add -A && git commit && git push`
   커밋 메시지 첫 줄은 `A1: 시맨틱 구조와 스킵 링크` 처럼 `항목ID: 제목` 형식.
   본문에 무엇을 왜 바꿨는지와 ChatGPT 교차검토 결과를 적는다.
   검증이 실패해 되돌렸다면 되돌린 상태 그대로 커밋하고 이유를 남긴다.
   원격: `origin` = https://github.com/taeyoun325/ai-company.git
   `projects/` 와 `logs/` 는 `.gitignore` 대상이니 산출물은 올라가지 않는다.

## 불변 규칙 (매 회차 지켜야 함)

- **API 키 등록 UI는 맨 마지막(C1)에 한다.** 그 전까지 mock 모드가 기본이다.
- 신뢰 경계를 약화시키지 않는다: DEV는 `src/`만 쓰기, `tests/`는 읽지도 못함.
- 완성도 점수에 QA 판단(통과율)을 다시 넣지 않는다. 순환논리가 된다.
- 외부 의존성을 UI에 추가하지 않는다. `web/index.html`은 단일 파일로 유지한다.
- 막지 못하는 위험은 README에 계속 정직하게 적는다. 지우지 않는다.
- **비밀값을 커밋하지 않는다.** 커밋 전에 `sk-ant-` / `AIza` 패턴을 훑는다.
  `tests_selftest.py` 의 `sk-ant-LEAK-CANARY` 는 의도된 가짜 값이니 예외.

---

## A. 접근성 — 지금 UI의 실제 결함

### A1. 시맨틱 구조와 스킵 링크
상태: 완료 (1회차)
지금 전부 `<div>`다. 스크린 리더로 영역 이동이 불가능하다.
`<main>`, `<aside>`, `<nav>`, `<h1>` 계층, 각 패널에 `aria-labelledby`,
"본문 바로가기" 스킵 링크. `lang="ko"`는 이미 있음.

### A2. 채팅 로그를 라이브 리전으로
상태: 완료 (2회차)
새 말풍선이 떠도 스크린 리더가 아무것도 읽지 않는다. 이 앱의 핵심 기능이 안 들린다.
`role="log"` + `aria-live="polite"` + `aria-relevant="additions"`.
말풍선마다 발화자를 접근 가능한 이름으로. 툴 호출 같은 소음은 `aria-live` 대상에서 제외 고려.

### A3. prefers-reduced-motion
상태: 완료 (2회차)
스캔라인이 8초마다 무한 이동하고, 맥박이 1.1초 주기로 뛰고, 말풍선마다 blur 전환이 있다.
전정기관 장애가 있으면 쓸 수 없다. `@media (prefers-reduced-motion: reduce)`로 전부 정지.

### A4. 대비비와 색 단독 인코딩
상태: 완료 (2회차)
`--faint:#3f5170`은 배경 대비 약 2.5:1로 WCAG AA(4.5:1) 미달. 모델명·타임스탬프가 여기 쓰인다.
그리고 통과/반려, 에이전트 구분이 **색으로만** 되어 있다. 색각 이상이면 구분 불가.
토큰 대비 수정 + 상태에 텍스트/도형 병기.

### A5. 키보드 완전 조작
상태: 완료 (2회차)
`.repo`, `.file` 버튼에 보이는 포커스 링이 없다. 코드 뷰어 모달은 포커스 트랩이 없어
Tab이 뒤로 새고, 닫아도 원래 위치로 포커스가 안 돌아온다. `role="dialog" aria-modal="true"` 없음.

### A6. 게이지·계기판 대체 텍스트
상태: 완료 (2회차)
완성도 SVG는 스크린 리더에 아무것도 아니다. 토큰 막대도 마찬가지.
`role="img"` + `aria-label`, 또는 숨김 텍스트로 수치 제공. 값 변경 시 과도한 낭독은 피할 것.

---

## B. Orca 계열 기능

### B1. 비밀키 브로커 (OrcaBot에서 가져옴)
상태: 완료 (3회차)
지금은 자식 프로세스 env를 세탁하는 수준이다. 한 단계 올린다:
키를 오케스트레이터 프로세스만 들고, 에이전트 코드 경로와 생성 코드 실행 경로 어디에도
평문으로 존재하지 않게 한다. 필요하면 키 접근을 단일 모듈로 좁히고 로깅·마스킹을 강제한다.
**네트워크 차단은 여전히 불가**라는 사실을 README에서 지우지 말 것.

### B2. 코드 뷰어 → 에디터 + diff
상태: 대기
지금은 읽기 전용 모달이다. 사람이 직접 고칠 수 있게 하고,
회차별로 파일이 어떻게 바뀌었는지 diff로 본다. 외부 라이브러리 없이 `<textarea>` + 자체 diff.

### B3. 산출물 미리보기 패널
상태: 대기
만들어진 게 웹앱이면 결과를 바로 본다. `src/`에 HTML이 있으면 iframe(sandbox 속성)으로 렌더.
없으면 진입점 실행 결과(stdout)를 보여준다.

### B4. 병렬 작업공간
상태: 대기
지금은 한 번에 한 프로젝트만 돈다. 여러 요구사항을 동시에 돌리고 탭으로 전환한다.
`bus`가 단일 전역이라 프로젝트별로 쪼개야 한다. 범위가 크면 쪼갤 것.

### B5. 협업 타임라인
상태: 대기
`logs/trace.jsonl`을 누가 언제 누구에게 무엇을 넘겼는지 가로 타임라인으로.
핸드오프와 재작업 구간이 눈에 보이게.

### B6. 예약 실행
상태: 대기
정해진 시각에 저장된 요구사항을 자동 실행. cron 문자열 대신 간단한 시각 지정.

---

## C. 마무리

### C1. API 키 등록 화면 — **맨 마지막**
상태: 완료 (3회차)
설정 화면에서 Anthropic / Gemini 키를 넣고 저장·검증. B1의 브로커를 통해서만 저장한다.
Gemini 모델 ID를 `models.list()`로 실제 조회해 고르게 한다(지금은 추측값이 박혀 있음).
여기까지 오면 mock이 아닌 실제 실행이 가능해진다.

### C2. 최종 점검
상태: 대기
README·BUILD_PLAN을 최종 상태와 맞추고, 자체 테스트 전체를 돌리고,
"막지 못하는 위험" 절을 최신화한다.

---

## 기록

(회차마다 아래에 한 줄씩 추가: 회차 · 항목 · 결과 · ChatGPT가 반박한 것 · 되돌린 것)

- **1회차 · A1 시맨틱 구조 · 완료**

  ChatGPT 교차검토는 **실패**로 처리했다. A1 질문 4개에 답하지 않고 직전 라운드(신뢰 경계)
  질문에 답했고, 마지막 줄의 "A1 계획은 그대로 진행해도 된다"는 근거 없는 동의뿐이었다.
  그래서 내 판단으로 진행하면서 **내 원안 하나를 스스로 기각**했다:
  패널마다 `section aria-labelledby`를 붙이려던 계획은 랜드마크를 6개 더 만들어
  탐색을 오히려 방해한다. 이름 없는 `section`은 랜드마크가 아니므로 h2만 남겼다.

  적용: 스킵 링크 / `main` + `aside` 2개(각각 aria-label) + `header` / `h1` 승격 /
  지표를 `dl`+`dt`/`dd` / 태스크·파일·저장소를 `ul`+`li` / 입력에 `label` /
  게이지 SVG는 `aria-hidden` 처리 후 시각적 숨김 텍스트로 수치 제공.

  검증: 자체 테스트 28개 통과, mock 파이프라인 완성도 100 정상.

- **2회차 · A2~A6 접근성 묶음 · 완료**

  20분 주기 cron이 02:40 이후 한 번도 발화하지 않았다(세션이 유휴가 아니었음).
  기다리는 대신 접근성 항목을 연속으로 처리했다. ChatGPT 교차검토는 이번에도
  직전 라운드 질문에 답하는 문제가 있어 생략하고 자체 판단으로 진행했다.

  - **A2** 로그를 `role="log" aria-live="polite"` 로. 판단 하나: **툴 호출 로그는
    낭독에서 제외**했다(`aria-hidden`). 파일 읽기/쓰기까지 전부 읽으면 정작 대화를
    따라갈 수 없다. 단계 전환은 별도 `role="status"` 로 짧게 알린다.
  - **A3** `prefers-reduced-motion` 으로 스캔라인·맥박·말풍선 전환을 전부 정지.
  - **A4** `--faint:#3f5170`(대비 약 2.5:1, AA 미달) → `#8ba3c4`(약 7:1).
    에이전트 색도 상향. 캐시 미적중 경고에 "⚠ 미적중" 텍스트 병기(색만으로 알리지 않기),
    판정 말풍선에 "· 판정" 배지.
  - **A5** `:focus-visible` 전역 포커스 링, 모달에 `role="dialog" aria-modal="true"`,
    **포커스 트랩**과 **닫을 때 원래 버튼으로 포커스 복귀**. 파일·저장소 버튼에 aria-label.
  - **A6** 에이전트 카드를 `article`+`h3`, 토큰 막대는 장식 처리하고 수치를 `dl`로,
    '작업 중/대기 중' 상태를 숨김 텍스트로 제공(발광 테두리는 색이라 안 들린다).

  **접근성 회귀 테스트 17개를 추가**했다(총 45개). 루프가 UI를 계속 고치므로
  스킵 링크·랜드마크·라이브 리전·포커스 복귀·낮은 대비 토큰 복귀를 못으로 박았다.

  검증: 자체 테스트 45개 통과, mock 파이프라인 완성도 100, 접근성 트리 육안 확인
  (complementary 2개·main·banner·log·form·dialog 모두 정상 노출).

- **3회차 · B1 비밀키 브로커 + C1 API 키 등록 · 완료**

  STATUS.md에서 권고한 순서(C1 먼저, 단 C1이 B1 브로커에 의존)대로 B1 → C1.

  **B1** `secrets_broker.py` — 키에 접근하는 유일한 지점.
  실질적 개선은 기동 시 `os.environ.pop`으로 **환경변수에서 키를 꺼내고 지우는 것**이다.
  이전에는 키가 프로세스 수명 내내 환경에 남아, 세탁을 빠뜨린 코드 경로나
  예외 트레이스백·디버거 덤프가 전부 유출구였다. `runner.py`의 세탁은 그대로 뒀다(중첩 방어).
  `scrub()`으로 로그·오류에서 키를 가리고, `mask()`로 화면에는 앞뒤 일부만 보낸다.
  한계도 적어뒀다: 같은 프로세스의 파이썬 코드는 `_store`를 직접 읽을 수 있다.
  파이썬에 진짜 캡슐화는 없으므로, 이건 *실수로 새는 경로*를 막는 장치다.

  **C1** 설정 다이얼로그 + `/api/settings*`.
  키 입력은 `type=password`, 저장 시 마스킹만 반환(원문은 다시 안 내려감),
  닫으면 입력값을 DOM에서 지운다. `.secrets.json` 저장은 선택이고 경고를 붙였다.
  **Gemini 모델을 실제로 조회해 고르게 했다** — `qa.list_models()`가
  `client.models.list()`로 사용 가능한 ID를 가져오고, 기본값 `gemini-2.5-pro`는
  "(미확인)"으로 표시된다. 지금까지 추측값이 박혀 있던 문제의 해결.
  접근성은 A5 패턴을 그대로 따랐다(role=dialog, 포커스 트랩·복귀 — 트랩이 두 모달을 모두 처리).

  **실제 버그 2건을 잡았다:**
  1. 키 저장이 500. 클라이언트 캐시를 비우려고 `agents.llm`을 import 하는데
     `anthropic` 미설치면 ModuleNotFoundError. 설정 화면은 SDK를 설치하기 *전에*
     쓰는 화면이므로 의존 순서가 뒤집혀 있었다. try/except ImportError로 분리.
  2. 검증 실패 메시지가 원인을 안 알려줬다. SDK 미설치는 별도로 안내하게 했다.

  또 하나: Windows에서 `pkill -f server.py`가 파이썬 프로세스를 죽이지 못해
  옛 서버가 계속 응답했고, 그래서 수정이 반영 안 된 것처럼 보였다.
  이후 `Get-CimInstance Win32_Process` + `Stop-Process`로 정리.

  브로커·설정 회귀 테스트 13개 추가 (총 58개).
  그중 핵심은 `test_broker_removes_keys_from_environ` — 키가 `os.environ`에
  되돌아오면 실패한다. `anthropic`·`google-genai` 설치 완료.

  검증: 자체 테스트 58개 통과, mock 파이프라인 정상, 설정 API를 가짜 키로
  왕복 확인(마스킹만 반환·원문 미노출·정리 정상), 다이얼로그 육안 확인.
